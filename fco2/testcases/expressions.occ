PROC p (VAL INT x, y, INT z)
  z := x + y
:

INT FUNCTION f (VAL INT x, y)
  VALOF
    SKIP
    RESULT x + y
:

PROC test.expressions ()
  INT a:
  INT b:
  INT c:
  SEQ
    a := 1
    b := 2
    c := f (a, b)
    c := (42 * a) + (b - (72 / c))
    p (a, b, c)
:
