process main (?uint8: in, !uint8: out, !uint8: err)
{
        [uint8]: alphabet;

        alphabet = ['a' .. 'z'];

        alphabet += ['A' .. 'Z'];
        
        alphabet += ['0','1','2'] + ['3','4','5'] + "6789";

	seqeach (c : alphabet)
	{
		out ! c;
	}
}
